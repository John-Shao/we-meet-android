package com.we.meet.ui.theme

import androidx.compose.material3.Typography

// Default Material 3 typography is fine for an MVP.  Wrap it so the
// WeMeetTheme has a single place to swap fonts later.
val JusiTypography: Typography = Typography()
