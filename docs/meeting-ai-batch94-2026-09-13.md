# 第 94 批：原生会中智能记录协议

接入明确 room UUID + LiveKit RM SID 的控制读取与开始/停止请求。开始必须显式 expected_run_id（可空），停止必须匹配已观察 run；严格校验冻结 result 和当前 current 的记录归属，允许历史回执与后续新 run 并存。coverage 只接受 unverified，不把文字送达当作完整音频覆盖。

ONLINE_CAPTURE 加密意图按账号、房间与真实场次独立寻址，同一房间新会议不复用旧意图。未知请求恢复原操作/key/body，权限暂失保留身份。入会令牌仅用于最小状态提示，单独 HTTP 客户端无账号认证/刷新/退出拦截、缓存、重定向、日志和自动重试；迟到响应需要来源仍有效。

验证：8 项会中协议 + 8 项分享协议 JVM 回归、3 项会中协调器 + 4 项加密存储隔离测试，共 23 项通过。默认 Debug/test APK、设计 token/diff 检查通过。无真实会议/模型/媒体调用，无迁移/默认开关变化。下一批接入会中控制与参会人提示。
