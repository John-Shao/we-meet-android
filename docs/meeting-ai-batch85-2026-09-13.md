# 第 85 批：人工纪要编辑与历史界面

资料详情展示人工纪要与修订历史，保留 AI 原版本。用户可以基于已有人工内容编辑，或显式确认改用当前列表首个 AI 版本作为草稿来源；保存新增修订，不覆盖旧版本。总览、决策、话题、行动项和待解决问题支持修改/增删，旧引用保留，新条目不伪造引用。

人工历史支持按修订分页及精确原文引用。只读用户可阅读与回溯；编辑按钮遵循当前 can_edit。后台暂停读取/隐藏编辑器，返回重新检查，撤权时隐藏正文和表单。未提交草稿仅保留在当前页面内存，离开页面或进程结束不会持久保存；已提交但结果未知的完整请求进入前批加密恢复通道。

保存冲突保留当前草稿并阻止覆盖新版本；结果未知要求先显式核对原 key/body，不发新请求。保存人工版本不会生成 AI 内容、任务或通知。

验证：6 项新交互 + 14 项资料页/总结控件回归通过，共 20 项；Debug/test APK、设计 token 和 diff 检查通过。浅色编辑器、保存态与深色只读态截图检查通过。无真实业务写入，无迁移/默认开关变更。下一批接入行动项确认与任务跳转。
