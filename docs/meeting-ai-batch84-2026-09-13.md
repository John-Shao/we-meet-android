# 第 84 批：人工纪要与确认任务协议

新增人工纪要当前版本、按修订分页历史、精确历史版本、显式保存，以及行动项任务状态/负责人搜索/确认创建接口。所有请求使用会议私有客户端，当前账号在响应前后核对。编辑能力缺失默认关闭，历史必须严格递减且精确匹配指定版本。

人工内容采用独立 DTO，保留精确引用与必需的 nullable end_ms，不向普通摘要点写入 action 专用字段。保存检查返回的内容、原 AI 版本及 expected_revision + 1，同时允许回执携带更晚的 current 版本。原生待提交正文上限 64,000 UTF-8 字节，以适配已有加密 intent 的 64 KiB 边界；更长内容提交前拒绝。

任务创建要求用户明确选择负责人 UUID 和有效日历日期（或 null），不解析 AI 人名/时间提示。跨版本相同行动项允许复用旧 review 的任务回执，已删除任务不自动重建。任务访问权限独立，task_id/status 可为 null。

新增 HUMAN_REVIEW/SUMMARY_TASK 加密意图种类，无数据库迁移。写入前持久化，未知 HTTP/畸形成功回执保留原 key/body；仅显式调用恢复，不在读取时产生任务或通知。正文不进入日志或备份。

验证：8 项新协议 + 7 项总结协议 JVM 回归通过；4 项新协调器 + 4 项加密存储模拟器测试通过，共 23 项。Debug/test APK、设计 token 和 diff 检查通过。未操作真实任务、消息、模型或业务网络。下一批连接原生人工纪要编辑与确认界面。
