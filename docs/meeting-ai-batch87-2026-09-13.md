# 第 87 批：原生会后问答协议与恢复

新增私有问题列表、精确问题读取和显式提问接口，固定原文 snapshot，不推断房间最新来源。成功回执必须匹配原问题和 snapshot；可回答结果要求非空回答及有效引用，无法回答只能为空答案/空引用，非成功状态不得携带旧答案。账号切换丢弃迟到私有响应。

问题意图在请求前写入独立 RECORD_QUESTION 加密种类，未知结果保留原 key/body；读取或恢复页面不触发模型请求。问题长度 1–2000，最近列表最多 10，答案最多 8000 字/12 引用，坐标严格验证。新增问答私有客户端保留不重定向/不缓存/不记录请求体，read 40s / call 45s 覆盖后端 30s 的模型尝试，超时仍按未知结果恢复。

验证：8 项新问答协议 + 8 项人工纪要协议 JVM 回归、4 项问答协调器 + 4 项加密存储模拟器测试，共 24 项通过。Debug/test APK、设计 token 和 diff 检查通过。无真实模型/网络请求，无迁移/默认开关变更。下一批接入问题来源选择与答案界面。
