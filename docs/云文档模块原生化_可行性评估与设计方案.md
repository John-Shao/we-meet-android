# 云文档模块原生化 —— 可行性评估与详细设计方案

> 立项日期:2026-09-06 · 范围:we-meet-android「云文档」tab 由 WebView 内嵌改为原生实现
> 关联文档:
> - 现状: `we-meet/docs/phases/p3-docs-app.md`(P3.5 云文档集成 App 客户端,WebView 方案的完整设计 + 踩坑录)
> - 战略: `we-meet/docs/extensions/客户端多端策略_对标飞书.md`、`企业协同套件路线图_对标飞书.md`(F 节「云文档」+ 路线图 P3「集成 La Suite Docs,不自建编辑器」)
> - 规范: `we-meet-android/docs/设计规范.md`(M3 + token + 共享组件 + 10 条自动护栏)
> - 事实盘点(调研稿): `docs/云文档原生化_调研_docs侧接口盘点.md`(docs 侧接口/功能全景)、`docs/云文档原生化_调研_android侧现状盘点.md`(App 侧现状/模块/规范)

---

## 0. TL;DR(结论先行)

| 问题 | 结论 |
|---|---|
| 能不能原生? | **能,但要分面看。** 文档的「管理/浏览/搜索/阅读/评论/版本/分享」全部可以、也应该原生;「实时协同编辑」是唯一的高成本高风险面,不建议本轮原生 |
| 为什么编辑最难? | docs 的内容 = **base64 编码的 Yjs(CRDT)更新**,不是 JSON;编辑 = BlockNote 块编辑器 + Yjs 绑定 + Hocuspocus WebSocket 协同 + Awareness 光标/评论锚点。JVM/Android 没有能直接用的 Yjs 实现(y-crdt 官方的 Kotlin 绑定 `ykt` 已停更、转 WIP 的 `y-uniffi`;Compose 生态无协同富文本编辑器),原生编辑 ≈ 自研「CRDT 集成层 + 协同块编辑器」,与路线图「不自建编辑器,CRDT 是最大隐藏成本」的既有决策正面冲突 |
| 推荐路线 | **原生外壳 + 原生阅读态 + 原生评论/版本/分享;编辑器画布保留 WebView(单页、收敛 chrome)**。视觉与交互不一致的部分(占比 ~90%)全部原生化,编辑画布作为「画布型」界面隔离 —— 与飞书/Notion/Google Docs 移动端的「编辑器是特殊表面」做法同构。**PC Web 与 Android 端同库、同会话体系、同 Hocuspocus 房间:分享/邀请/搜索/深链/实时协同天然互通,按 §4.7 的入口矩阵逐条打穿并验收** |
| 工作量 | M1 原生外壳 ≈ 33–46 人日 · M2 原生阅读态 ≈ 34–46 人日 · M3 编辑画布(WebView 单页)≈ 15–22 人日 · 合计 **≈ 82–114 人日(2 人并行约 9–11 周)**;全原生编辑(M4)另需 **≥ 75–130 人日且高风险**,单独立项、先做技术验证(Spike)再拍板 |
| UX 规范 | **硬性要求,逐条映射**:《设计规范.md》(M3 + token + 组件 + 三态 + 文案 + 无障碍 + §7 清单 + `checkDesignTokens` 10 条护栏)与《app-ux-device-matrix.md》(横竖屏/字体缩放/折叠屏/insets/键盘)全覆盖;文档内容区样式走**新增语义 token + 对比度实算单测**,豁免边界限定在 renderer 包并注明理由(§4.11) |
| 后端改动 | **主路径零改动**(README 级):复用 `POST /api/v1.0/docs/session/` 票据 + docs `session-from-ticket` 会话引导 + docs 现成 REST;可选小改(+1–3 人日):`docs/session/` 返回票据明文以便原生会话引导 |
| 落地形态 | 新 `feature-docs` 模块(对标 `feature-im` 的模块划分与实现范式),接入 `core-design`(token/组件/护栏)与 `core-directory`/`feature-im`(人员选择、分享入口);WebView tab 保留为「降级/兜底」入口 |

---

## 一、现状盘点

### 1.1 云文档(we-meet-docs)是什么

docs = [suitenumerique/docs](https://github.com/suitenumerique/docs) 的分支(镜像名 `app-impress` 5.4.1),三件套独立部署:

- **后端**(`src/backend`,Django 5 + DRF):文档树(`treebeard` MP_Node,`path` + `steplen=7` 的 base62 手册排序)、软删/恢复(回收站)、收藏、链接分享(`link_reach`/`link_role`,与祖先合成 `computed_link_reach/role`)、访问控制(`DocumentAccess` 按 path 前缀继承;角色 reader/commenter/editor/administrator/owner)、邀请(邮箱邀请,首次登录转 access)、评论/线程/表情、`abilities` 细粒度权限字典。
- **内容存储**:S3(OSS),key 为 `{doc_id}/file`,内容为 **base64 文本,解码后是 Yjs 二进制更新**(不是 JSON!);历史版本 = S3 对象版本(`versions/` 端点直接透传)。无独立 Version 模型,恢复版本 = 取旧版本内容 PATCH 回 `content/`。
- **协同服务**(`src/frontend/servers/y-provider`):单进程 Express + **Hocuspocus**,WS 路由 `GET /collaboration/ws/?room=<doc-uuid>`(房间名 = 文档 UUID);`onConnect` 转发请求到后端 `GET /documents/{id}/` 拿 `abilities`,据此设 `connectionConfig.readOnly`;从 `Cookie: docs_sessionid` 取 sessionKey。**Updates 只在内存**,持久化靠前端每 60s / 离开页 `PATCH /documents/{id}/content/`(base64 Yjs 更新)。另有 `/api/convert/`(markdown / Yjs / BlockNote JSON ⇄ 互通)。
- **前端**(`src/frontend/apps/impress`):Next.js 16(Pages Router,静态导出)+ **BlockNote 0.51**(TipTap/ProseMirror 之上的块编辑器)+ `@hocuspocus/provider` + Yjs;评论锚定在 Yjs 文档内的「comment mark(threadId)」,DB 只存 body/metadata;离线:service worker(workbox)+ idb + 乐观保存。
- **we-meet fork 的改动**(已上线,与 App 强相关):`users/session-ticket/`(s2s 单据)+ `session-from-ticket/`(一次性票据换 docs 会话,60s 有效、用一次即废,见 `core/api/session_bootstrap.py`);`search-for-user/list-for-user/grant-access-for-users`(s2s,按 `sub` 定位用户);`formatted-content`(上游已有,保留);内嵌收敛(UA 标记 `WeMeetApp`、`?embed=1`、`?chrome=none`、`wemeet-*` 消息协议、主题/语言跟随);CSP `frame-ancestors` 白名单;zh-CN 语言与国内 OSS 兼容。

### 1.2 App 端现状(we-meet-android)

- 纯原生 Kotlin + Compose + Material 3,6 个底部 tab:消息 / 日历 / 会议 / 通讯录 / **云文档** / 任务(`MainTabScreen.kt` 的 `MainTab` 枚举)。多模块:`app` + `core-design`(token/组件/护栏)+ `core-directory` + `feature-im` + `feature-assistant`;Retrofit 2.11 + Moshi + OkHttp 4.12、Compose BOM 2024.10、AGP 8.7.3 / Kotlin 2.0.21;**无 DI 框架**——`WeMeetApp` 手工装配,AFeature 模块经 `Deps` 接口注入(`ImDeps/AssistantDeps/DirectoryDeps/CallHost`),ViewModel 用 `Provider.Factory`;**无 Room/DataStore**——持久化 = EncryptedSharedPreferences(token)+ SharedPreferences + JSON 文件(`HistoryStore`)+ 内存 StateFlow(`UserDirectory` 1h TTL 一类);Getui 推送、PostHog、**minSdk 29 / targetSdk 34**(`app/build.gradle.kts`,README/CLAUDE.md 的 24 已过时)。
- 云文档相关代码体量:WebView 集成约 **1400 行**(`ui/docs/DocsScreen.kt` 565 + `DocsViewerScreen.kt` 96 + 登录 WebLoginScreen 306 + MainTab 集成 ~90 + AppNav ~25 + 会话/搜索 API 82 + IM 侧 doc-card/选择器 ~320),其中加载/桥接/会话引导约 780 行 —— 原生化的**回收面**与**替换成本**都以此为基数。
- 「云文档」= **常驻 WebView**(实例在 `MainTabScreen` 层持有、预加载)+ 独立 `DocsViewerScreen` 深链阅读 WebView(`chrome=none` 阅读态)。已实现并上线:
  - 登录:WebView 内 Keycloak OIDC(PKCE)登录,进程级 CookieManager 共享 KC 会话 cookie → docs tab 免登;另有 `POST /api/v1.0/docs/session/` 票据兜底(`DocsApi.createSession`),不依赖 KC 会话。
  - 嵌入协议:UA 标记 + 主题/语言跟随 + `DocsWebViewClient`(拦截导航、面板状态)+ `DocsHostBridge`(`WeMeetHost.postEvent`:分享到聊天、全局搜索、`wemeet-embed-hello/host-hello` 能力握手)+ 分享文档到聊天(DocPickerDialog/DocCardBubble)。
  - 全局搜索已接入 docs 代理(`SearchApi.searchDocs` → meet `GET /api/v1.0/docs/search/`)。
- **现状痛点**(用户视角):
  1. **风格割裂**:docs tab 是 Web 站点 Chrome(悬浮面板、Web 列表项、Web 弹窗/抽屉、Web loading),与其它 5 个原生 tab 不是一套语言;
  2. **交互割裂**:返回键层级、长按、下拉刷新、多选、手势、暗色模式下的加载/空态都与原生不一致;Web 交互在手机上偏桌面(行内菜单、hover 态);
  3. **体验问题**(p3-docs-app.md 记录的):KC SSO 会话 10h/30min 过期 → 隔天首开要输一次 OTP;language/theme 三仓(meet/docs/App)强耦合,靠 UA/桥同步;WebView 加载慢、内存大、无法离线;
  4. **维护成本**:双 UA 标记、桥协议版本、docs 镜像收敛逻辑都散在「App ↔ docs 镜像」两处,需三端同步发版。

### 1.3 不做的范围(本评估)

电子表格(Sheets)、多维表格(路线图已明确不做)、模板、AI 助手/工具栏(BlockNote AI、`ai-proxy` 等)、PDF/DOCX/ODT 导出、房间内文档侧栏、按事件建文档模板。这些维持 WebView/Web 现状。

---

## 二、可行性评估(分层)

### 2.1 数据面:接口就绪度(结论:高)

docs 的 REST 面完整,除以 `session cookie + CSRF` 为认证外没有障碍:

**认证三通道**

| 通道 | 用途 | App 可用性 |
|---|---|---|
| `SessionAuthentication`(session cookie `docs_sessionid` + `X-CSRFToken`) | 全部用户 API(documents CRUD/children/tree/versions/favorite/link/trashbin、accesses、invitations、threads/comments、ask-for-access、users/me、search、attachment-upload、media-check、formatted-content) | **✅ 可用**。引导链路已存在:App 用自己的 Bearer 调 `POST /api/v1.0/docs/session/` 拿带上票据的 URL → 原生 OkHttp 直接 GET `session-from-ticket/?ticket=…`(带 CookieJar,跟随 302)→ 拿到 `docs_sessionid` + `csrftoken` → 后续请求带 cookie + `X-CSRFToken`(写操作)。Django 会话 12h(`SESSION_COOKIE_AGE`),可静默续期 |
| `ServerToServerAuthentication`(meet 后端持静态 Bearer) | s2s 端点:`users/session-ticket/`、`create-for-owner`、`search-for-user`、`list-for-user`、`grant-access-for-users` | ✅ 已由 meet 后端代理(不走 App 直连) |
| `external_api`(`/external_api/v1.0/`,加密 JWE) | 服务账号级 documents/users | ⚠️ 不适用:它是 s2s 通道,身份不是用户,App 不应持有 |

**关键事实(fact 清单)**

| 能力 | 端点/机制 | 原生可行性 |
|---|---|---|
| 文档列表/筛选/排序/分页 | `GET documents/?is_creator_me=&is_favorite=&q=&title=&ordering=&page=(≤200)` | ✅ 列表返回 `abilities/user_role/is_favorite/excerpt/updated_at/numchild/depth/creator…` 足够列表 UI |
| 文档树(懒加载) | `GET documents/{id}/children/?page=`、`GET documents/{id}/tree/` | ✅ 左栏树可原生(懒加载分页) |
| 文档详情 | `GET documents/{id}/`(含写 LinkTrace) | ✅ |
| **阅读正文** | `GET documents/{id}/formatted-content/?content_format=json\|markdown\|html`(y-provider 服务端转换) | ✅ **这是本方案的最大支点**:`json` = BlockNote JSON(结构化、保样式),`markdown/html` 为保底。⚠️ 依赖 y-provider 服务可用(部署监控点) |
| 图片/附件 | `attachment-upload/`(multipart)+ `media-check/` + `MEDIA_BASE_URL/media/{pk}/attachments/…`(nginx media-auth 子请求,需 docs 会话 cookie) | ✅ OkHttp/Coil 带 cookie 直取;上传走 multipart |
| 搜索 | `documents/search/?q=`(La Suite Find 索引器,未配置回退标题 ILIKE) + meet 代理 `api/v1.0/docs/search/` | ✅ 原生搜索页;全局搜索继续用 meet 代理 |
| 收藏/移动/复制/重命名/删除/恢复 | `favorite/`、`move/`、`duplicate/`、`PATCH documents/{id}/`、`DELETE`、`restore/`、`trashbin/` | ✅ |
| 分享(链接权限/成员/邀请/申请访问/离开) | `link-configuration/`、`accesses/`、`invitations/`、`ask-for-access/`、`leave/`、`users/?q=` | ✅ |
| 评论/线程/表情 | `threads/`、`comments/`、`reactions/` | ✅ 列表/发表/回复/解决/表情全 REST。⚠️ **内容锚定(高亮定位)在 Yjs comment mark 里**,原生侧只能显示「评论列表 + 引用文本摘要」;点击定位 → 跳编辑画布(WebView) |
| 版本(列表/查看/恢复) | `GET documents/{id}/versions/`、`versions/{vid}/`、恢复 = PATCH `content/` | ✅ 列表 + 恢复原生;**版本内容查看** = 原 Yjs 更新 → 需 Yjs 解码,本轮用 WebView 兜底(或后续 yrs 绑定) |
| **实时协同** | `GET /collaboration/ws/?room={uuid}`(Hocuspocus,y-websocket 二进制协议;`docs_sessionid` cookie 鉴权;服务端按 abilities 定 readOnly) | ⚠️ 协议可对接,但客户端需要 **Yjs 实现** —— 见 2.2 |
| 新建文档 | `POST documents/`(可带 `file` 走转换导入;空白文档无需 content) | ✅ 原生「新建」= 建空文档 → 跳编辑画布填充 |

### 2.2 编辑面:技术可行性(结论:唯一高风险区,且与既有战略冲突)

要原生编辑,必须满足其中一条:

1. **读取/生成 Yjs 更新**:编辑最终落库 = `PATCH content/`(base64 Yjs 更新);阅读 = 解码 Yjs(BlockNote `document-store` 与 `comment-threads` 两个顶层集合 + y-prosemirror 绑定)。不依赖 JS 的 Yjs 实现在 JVM 上:
   - `y-crdt`(Rust `yrs`)有官方 **Kotlin 绑定 `ykt`,但已声明「not active,参见 y-uniffi」**;`y-uniffi` 处于 **WIP**(UniFFI 生成,官方支持 Swift,README 提到 Kotlin 为演进方向,功能未全暴露);
   - 现实路径 = 自建 `yrs` 的 Android JNI/UniFFI 绑定(NDK 交叉编译 arm64-v8a/armeabi-v7a/x86_64 + 发布 AAR + 跟随上游版本),或接受「JNI + 自维护」;
   - **结论:没有开箱即用的 JVM Yjs;自建绑定属基础设施投资,3–5 人周仅打通,且是长期维护包袱。**
2. **BlockNote/ProseMirror 兼容的编辑器**:Compose 生态的富文本编辑器(`com.mohamedrejeb.richeditor`、`halilozercan/compose-richtext` 等)支持基础 span/段落/列表/链接,但**没有**块模型(BlockNote 的 block schema:多列、表格、视频/PDF 块、互链、任务列表、callout…)、没有 Yjs 绑定、没有协同、没有评论 mark、没有 undo/redo 对齐。若做,需自研「BlockNote Yjs 结构 ↔ Compose 块渲染/编辑」双层映射(源码级移植 BlockNote 的 Yjs binding 语义)。
3. **并发语义**:整文档 PATCH(最后写入者胜)会**覆盖 Web 端正在实时协同编辑的内容**;后端有 `COLLABORATION_WS_NOT_CONNECTED_READ_ONLY` 锁(无 WS 连接时单写者缓存锁),但「多端同时编辑」仍然丢更新。原生编辑若不做 y-websocket 客户端,只能定位为「单人/离线轻改」,且必须处理与 Web 协同者的冲突(乐观锁 / 失败重试 / 提示合并)。

**结论:全原生实时协同编辑器 = 自研一个「Yjs CRDT 集成层 + 协同块编辑器」,75–130+ 人日起步、跨版本维护、与「不自建编辑器」战略冲突。不可行于当前资源;列为远期 M4 门禁(Gate),先做 5–10 人日 Spike 验证 yrs-JNI 通路再决策。**

### 2.3 界面面:可行性(结论:高)

- 全部列表/详情/弹层/抽屉模式在 App 现有模块都有范式(`feature-im` 的会话列表/详情/更多菜单、`ui/tasks` 的任务列表/编辑、`ui/calendar` 的层级选择、`core-directory` 的人员选择器);
- 设计系统与护栏完备(`设计规范.md` + `checkDesignTokens` 10 条规则);
- 阅读态渲染器:BlockNote JSON → Compose 是一个**确定性的节点映射**(约 20–25 种块 + 行内标记),中等难度、可单测、可快照测试;特殊块(PDF/视频/多列)降级为「卡片 + 用网页版查看」。

---

## 三、方案对比

| | A. 全原生(推荐远期目标) | **B. 原生外壳 + WebView 编辑画布(推荐本轮)** | C. 维持 WebView,仅做套壳优化 |
|---|---|---|---|
| 一致性 | 100%(理论与 Web 对齐) | **~90%**:管理/浏览/阅读/评论/版本/分享全原生;编辑画布单页收敛 | 30%(还是 Web 站点 Chrome) |
| 编辑能力 | 与 Web 完全等价(实时协同/光标/评论锚定) | 等价(编辑画布就是 docs 编辑器,只是换了宿主壳) | 现状 |
| 工作量 | ≥ 130 人日 + 高风险 | **82–114 人日** | 10–20 人日(深浅不一) |
| 风险 | CRDT 数据兼容、编辑部维护、多端一致性 | 低:数据链不变、编辑链路不变 | 低但体验问题无解 |
| 与战略 | 冲突(「不自建编辑器」) | 一致(编辑核心保留在 docs) | 一致 |
| 长期 | 编辑部可做成 M4 渐进(先只读渲染 → 轻编辑 → 协同) | 为 M4 预留了接口(阅读态渲染器是编辑器的自然前身) | 没有升级路径 |

---

## 四、详细设计方案(选 B)

### 4.0 目标与范围

**目标**:云文档模块与其余 5 个原生模块同语言、同交互、同规范;离线可用(浏览/阅读);编辑体验不降级(Web 画布);**与 PC Web 端无缝互通**(分享/链接/搜索/实时协同/权限,见 §4.7)。
**范围(本轮)**:

| 面 | 原生 | 保留 WebView |
|---|---|---|
| 文档列表(全部/我的/收藏/最近/回收站)、筛选、排序、分页、下拉刷新、长按多选 | ✅ | |
| 全局搜索 + 文档内搜索 | ✅ | |
| 新建空白文档、重命名、移动、复制、删除、恢复、收藏 | ✅ | |
| 文档详情:头部(emoji 图标/标题/作者/时间/状态) | ✅ | |
| **阅读态**(BlockNote JSON 渲染) | ✅ | 特殊块降级入口 |
| 分享(链接权限/成员/邀请/申请访问/离开/复制链接) | ✅ | |
| 评论/线程/表情(列表 + 发表 + 解决) | ✅ | 点击锚定跳编辑画布 |
| 版本(列表 + 恢复) | ✅ | 版本内容预览 |
| **跨端入口统一**(IM doc-card / 全局搜索 / 会议纪要链接 → 原生;PC 分享/邀请 → 列表可见) | ✅ §4.7 | 无法解析的链接走浏览器/WebView |
| **编辑态(富文本画布)** | | ✅ 单页 `?chrome=editor` |
| AI、导入导出(PDF/DOCX/ODT)、模板、电子表格 | | ✅ 入口从原生跳 Web 页面/兜底 |

### 4.1 架构总览

```
:app (入口/导航/tab)
 └── :feature-docs(新)
      ├── ui/        DocsHomeScreen · DocsGridFilterSheet · DocDetailScreen
      │              DocReaderScreen(阅读态) · DocEditorCanvas(WebView 画布)
      │              DocCommentsSheet · DocVersionsSheet · DocShareSheet
      │              DocCreateSheet · DocMoveSheet · DocSearchScreen
      ├── data/
      │   ├── net/   DocsSessionManager(票券→会话 cookie→CSRF) · DocsApi(REST)
      │   │          DocsMediaLoader(Coil+同一 CookieJar) · CollabWsUrlProvider
      │   ├── dto/   DocumentDto · AbilitiesDto · ThreadDto · VersionDto …
      │   └── repo/  DocumentRepository · TreeRepository · CommentRepository…
      ├── domain/    渲染模型: DocBlock 密封类(BlockNote JSON → 结构化树)
      └── renderer/  BlockNoteJsonRenderer(≈20 块) · 特殊块降级
依赖: core-design(全部 token/组件/护栏;**扩展点:新增语义色走 `WeMeetExtras.docs`,见 §4.11.2**) · core-directory(人员选择)
      feature-im(data 层的 DocCard API,分享到聊天) · app(导航/深链/主题)
```

设计参数:沿用 `feature-im` 的「data/domain(如有)/ui + Repository + StateFlow<UiState> + LazyColumn + 三态成组」范式;模块边界同样走 **`DocsDeps` 接口注入**(宿主 `WeMeetApp`/`MainTabScreen` 实现:提供 `shareToChat(doc)`——复用 `feature-im` 的 `ForwardPicker`、`pickUser(s)`——复用 `core-directory` 的 `ContactPicker`、`openDoc(url)` 深链放行、`languages` 取应用语言);持久化沿用现有模式(SharedPreferences/JSON 文件 + TTL,不引入 Room/DataStore);不做新 DI 框架;模块内所有用户可见文案进 `strings.xml`(5 语言键数对齐:values/values-zh-rCN/values-de/values-fr/values-nl,中英必齐)。

### 4.2 认证与会话(核心新部件:`DocsSessionManager`)

两个独立会话栈并存,**互不干扰**:

| 栈 | 使用者 | 引导 |
|---|---|---|
| WebView 栈(现状) | 深链兜底 / 特殊块降级 / 编辑画布 | `Bearer → POST /api/v1.0/docs/session/ → ticket URL`(或 KC SSO) |
| **原生栈(新)** | 全部原生 REST + 媒体 + (远期)协作 WS | `Bearer → POST /api/v1.0/docs/session/` 拿票据 URL → OkHttp(CookieJar)GET `session-from-ticket/?ticket=…` 跟随 302 → cookie:`docs_sessionid`(+`csrftoken` 由后续响应 Set-Cookie/第一次 GET 获得) |

**会话生命周期**:
1. 冷启动/首次进入 docs tab:静默建会话(`DocsSessionManager.bootstrap()`,单飞,60s 超时);
2. 请求 401 → 重新 bootstrap 一次;403 → 按 `abilities` 呈现只读/申请访问态(不弹登录);
3. Django 会话 12h 有效;App 长登录期间每次会话过期自动重建(体验优于现状:现状 KC SSO 30min idle / 10h max 后会看到登录页);
4. 登出(App 级)与 WebView 栈的 cookie 清理保持一致(`signOut` 现有逻辑延长到原生栈 CookieJar);
5. 写操作:统一 `X-CSRFToken`(从 `csrftoken` cookie 读;每个非安全请求带),CSRF 失败走重取 cookie → 重试一次。
6. **存储**:原生栈的 `docs_sessionid`/`csrftoken` 存 **EncryptedSharedPreferences**(与 `TokenStore` 同库),不落明文。

**可与后端协商的小增强(1–3 人日,可选)**:`POST /api/v1.0/docs/session/` 的响应增加 `ticket` 字段(纯字符串)或新增 `/api/v1.0/docs/session/cookie/`,让 App 不必解析 URL、也不必先 GET 一次。不改也可用。

### 4.3 数据层(API 映射)

| 原生屏/动作 | 端点 | 备注 |
|---|---|---|
| 主页「全部」 | `GET documents/?ordering=-updated_at`(分页 20/页,page_size≤200) | 原生分页 + 增量加载;`abilities` 决定行内操作可用性 |
| 「我的」/「收藏」 | `?is_creator_me=true` / `?is_favorite=true` | 主页筛选条;默认排序即「最近」 |
| 回收站 | `GET documents/trashbin/` | 恢复 `restore/`;彻底删除按 `abilities` 开放 |
| 树(左栏/移动选择器) | `children/` 懒加载分页 + `tree/` 祖先链;提交 `move/` | 移动:懒加载树弹层 + 当前节点禁用 |
| 详情头 | `GET documents/{id}/` | title/作者/时间/状态;**emoji 图标 = 标题首字符 emoji 的解析**(与 docs 前端 `getEmojiAndTitle()/updateDocEmoji()` 同语义:emoji 存于 title 字符串,修改即 PATCH `title`),原生侧实现等价解析函数 |
| 阅读正文 | `GET documents/{id}/formatted-content/?content_format=json` | 缓存:etag/304(`If-None-Match`);失败 500(y-provider 不可达)→ 降级 `content_format=markdown` → 再失败 → 阅读态 WebView |
| 评论 | `GET/POST documents/{id}/threads/`、`/threads/{tid}/comments/`、`reactions/`、resolve | 列表/发表/回复/表情/解决全原生;正文锚定高亮跳编辑画布 |
| 版本 | `GET versions/` + 恢复;查看旧版本 | 查看 → WebView(或远期 yrs 解码) |
| 分享 | 链接权限 `PUT link-configuration/`;成员 `accesses/` CRUD;邀请 `invitations/`;申请 `ask-for-access/`;离开 `leave/` | 三档 reach × 三档 role 选择 UI(对齐 Web「不可比父更宽」约束,用 `computed_link_*` 限制) |
| 搜索(模块内) | `GET documents/search/?q=` | 与全局搜索(meet 代理)同口径 |
| 媒体 | `MEDIA_BASE_URL/media/{pk}/attachments/…` | 走原生 CookieJar(同 docs 会话);Coil `NetworkHeaders` 或自定义 `OkHttpClient`;上传 `attachment-upload/` + `media-check/` 轮询 |
| 新建 | `POST documents/`(title,空内容)→ 跳编辑画布 | |
| 导入 | `POST documents/`(multipart `file`)+ 上传后跳编辑画布 | 列表入口「导入」(docx/txt/md 等,受 `CONVERSION_*` 配置限制) |

**错误语义**:
- 401 → 重建会话重试一次 → 仍失败 → 全局登录态失效(走现有登出);403 + `abilities.retrieve=false` →「申请访问」态(ask-for-access 有现成流);403 读不到 → 从列表隐藏(与 Web 同口径:列表本身就是按可见性过滤的)。
- `429` → 列表节流`180/hour`(users 搜索专有)与 docs 节流按配置;出现时静默退避 + Toast。

### 4.4 UI/交互设计(对齐 `设计规范.md`)

**文档主页(`DocsHomeScreen`,tab 根页)**
- 顶栏:`WeMeetTopBar(title=「云文档」)`。右侧:搜索(放大镜)、「+」(新建/导入)。**不用 `design-exempt`**:搜索入口是图标按钮,不加搜索框。
- 过滤器条:`全部 | 我的 | 收藏` 分段 chip/SegmentedButton + 排序(最近更新/创建/标题);回收站放更多菜单;与 `ui/tasks` 的「筛选/分组与排序」行同构。
- 列表(卡片风格对齐 `feature-im` 会话列表):文档图标(按内容类型区分:空白文档/表格等,取 `doc_type`/abilities——**note**:docs 目前只有文档,无 Sheets;图标占位为「文档」,后续类型扩展只在 DTO 加字段)、标题(titleMedium 单行省略)、副文案(bodySmall:更新时间 + 摘要 excerpt 首行)、右侧「···」更多菜单(`DocCardActionsSheet`)。空态/加载/错误三态成组(`WeMeetLoading/Empty/ErrorState`)。
- 长按 → 多选模式(顶栏变操作条:收藏/移动/复制/删除/转发),对齐 `ui/tasks` 长按交互。
- 下拉刷新 + 分页(page 20),下拉刷新增缓存失效。

**文档详情(`DocDetailScreen`,二级页)**
- 顶栏:`WeMeetTopBar` 返回 + 「分享」「···」(收藏/移动/重命名/复制链接/删除/导出/版本)。
- 头部:`DocCoverInfo`(标题 titleLarge 可编辑(内联输入,保存 `PATCH title`)、创建者头像+名字、更新时间、「已读/未读」类状态徽标(若有))。
- 正文:`DocReader`(阅读态,见 4.5)。
- 底部操作条(可选):评论(角标未读数)、版本、分享(主按钮)。

**评论(`DocCommentsSheet`)**:BottomSheet 列表(头像/名字/时间/正文/表情条/回复);顶部线程状态(已解决/未解决);发表框含 @(复用 `feature-im` 的 MentionAliases/选择器);回复/表情/解决/删除(权限按 `abilities`)。**锚定点** → 「在文档中查看」按钮 → 调用编辑画布定位(见 4.6 桥)。

**版本(`DocVersionsSheet`)**:时间 + 作者/大小 + 「查看」「恢复」;恢复二次确认(`DestructiveConfirmDialog` 语义,操作名「恢复此版本」)。查看 = 打开阅读态 WebView(带版本参数,需 docs 镜像支持 `?version=`;**未支持则先用「恢复为当前内容」预览?不,预览不做,版本查看按钮仅在 WebView 可用时显示**)。

**分享(`DocShareSheet`)**:三段:①链接权限(可达范围 restricted/authenticated/public × 角色 reader/commenter/editor,禁用项按 `computed_link_*`);②成员列表(accesses:角色标签/移除/改角色,「添加成员」→ 搜索 `users/?q=` + 复用 `core-directory` 选择器;③邀请(邮箱,`invitations`);④「申请访问」状态与「离开文档」。内嵌「分享到聊天」直接内联为原生入口(复用 `feature-im` DocPickerDialog 反向:「选出会话」)。

**新建/移动**:新建 = 弹层(标题输入 + 「创建」);创建后跳详情(空文档)→ 用户可进编辑态。移动 = 树选择器弹层(懒加载 + 当前节点禁用)。

**搜索**:模块内搜索页 = `DocsSearchScreen`(TopBar 内 SearchBar + 结果列表,滞后 300ms 防抖、最小长度提示、状态三态);结果项 = 文档行 + 父路径;点击进详情。

**无障碍/规范**:所有图标 contentDescription;触控区 48dp;破坏操作 `DangerButton` + 二次确认(删除、移动出权限、离开文档);新颜色对实算对比度(正文 4.5:1、图标/底 3:1);深色模式全覆盖;文案全部 strings.xml;长文本省略。

### 4.5 阅读态渲染器(`BlockNoteJsonRenderer`)

**输入**:`formatted-content?content_format=json`(BlockNote JSON 块数组)。**输出**:Compose 递归渲染。

- 块映射(先满足「见过就渲染」,逐类加):paragraph / heading(level1-6)/ bulletList / numberedList / todoList(taskItem + 勾选框)/ quote / codeBlock(带语言标签 + 单行等宽字体 + 复制按钮)/ table(表头、行列合并、单元格对齐与背景色,用 Compose Canvas 或嵌套 Row/Column;复杂表格降级「截断 + 打开 Web」)/ image(Coil + 点击全屏 Lightbox,参考 `feature-im` ImageLightbox)/ video / file(附件卡片,点击预览/下载,同 `feature-im` 附件样式)/ callout(图标 + 左色条)/ multiColumn(按列数渲染,窄屏单列叠加 + 提示)/ pdf(降级卡片)/ uploadedFile、linkToDoc(卡片:图标 + 标题 + 打开)→ 路径链接(interlinking,点击跳原生详情)/ divider / pageBreak(提示)/ 行内:bold/italic/underline/strike/code/highlight/link/mention(@渲染为 Chip 样式,点击查人员卡)/ comment mark(渲染为浅底高亮 + 点击进评论线程)/ emoji。
- **样式策略**:沿用 `design` 与 docs 的「编辑器内容风格」一致性 —— 正文 bodyLarge、标题按 level 取 type scale(`headlineSmall/titleLarge/titleMedium`…),引用/代码/表格用 container 色 + `onSurfaceVariant`;**所有取色走 token**(新增「文档内容」语义色进 `WeMeetExtras.docs.*`),表格/代码块背景属「文档内容样式」——护栏若误报,使用 `// design-exempt: 文档内容区按编辑器语义样式`(与现有 `docs/设计规范.md` 的豁免先例同格式,需在 PR 说明)。
- **降级规则**(渲染器永远不崩):未知块/未知 mark → 渲染为一个「块占位卡片」+ 文案「该内容暂不支持在客户端预览」+「用网页版打开」按钮(跳阅读态 WebView);图片加载失败 → 占位 + 重试;文档过长 → 视口内分块 + 懒加载图片,避免超长 LazyColumn 崩。
- **空文档态**:空白正文 → 「这是一篇空文档,点击开始编辑」的引导空态(与 docs Web 一致)。
- 性能:文档 JSON 可能数百 KB;解析在 IO/后台(协程 + 结构复用),渲染用 LazyColumn(块级)+ 记忆化;大图 Coil 缩放。

### 4.6 编辑画布(WebView 单页:`DocEditorCanvas`)

**形态**:详情页内「阅读态 ↔ 编辑态」切换(用编辑按钮/点击正文进入;顶部保持原生 `WeMeetTopBar`,工具栏/浮层属 docs 编辑器)。编辑态 = 一个**独立轻量 WebView**(复用 `createDocsWebView` 全套配置:MATCH_PARENT、UA 标记含 `theme=`,允许域拦截、`WebViewClient` 先于 `loadUrl`、`deferInitialLoad`),直载:`https://docs…/?embed=1&chrome=editor&lang=<app语言>`(进站走票据 URL,复用 `loadDocsTabEntry`/`docsEntryUrl` 既有逻辑;主题走 UA 标记,与 tab WebView 同机制)。

**docs 镜像侧需要的新增(小,镜像内)**:`?chrome=editor` 收敛:隐藏 docs 站点级 Chrome(顶部栏/左栏/右栏/网格/用户区),只保留 **BlockNoteEditor + 其原生工具栏浮层 + Comments 浮层/ThreadsSidebar**(在编辑器内) —— 即「无站点壳的纯编辑器」;编辑器外的路由(如 AI、导出、分享弹窗)入口全部交给宿主(已有 `sendToHost` 通道,**扩展必须走新增 message type**——`DocsHostBridge` 约定只保留一个 `WeMeetHost.postEvent(String)` 方法,新命令一律加 type,未知 type 静默忽略,老 App 与老镜像互不破坏)。

**桥接协议增量(全部为新 type,向后兼容)**:
- docs → 宿主:`wemeet-editor-dirty`(保存队列未同步)、`wemeet-editor-ready`(进编辑态首帧)、`wemeet-open-comment(threadId)`(点击评论锚点回宿主)、`wemeet-export-request(format)`、`wemeet-ai-request`;
- 宿主 → docs:`wemeet-edit-mode`(进入/退出编辑)、`wemeet-navigate-comment(threadId)`(宿主请求画布定位评论)、`wemeet-save-now`(退出前强制落库,承接 docs 自身 60s PATCH)、`wemeet-set-locale`。

**宿主(App)职责**:
- 标题/emoji 编辑放原生(编辑框在 WebView 上方,保存走 `PATCH documents/{id}/`),WebView 只专注正文;
- 返回键:WebView 内有浮动工具栏时优先关浮层(现有 `wemeet-panel-state` 复用即可);**脏检查**:docs 侧上报 `wemeet-editor-dirty`,宿主在返回前弹「有未保存更改」选择(继续编辑/放弃/保存并退出),保存请求下发 `wemeet-save-now` + 60s PATCH 兜底(与 docs 自身 `useSaveDoc` 语义对齐);
- 主题/语言/深色:复用现有桥(`embedderTheme/embedderLanguage` 已工作);
- 只读:`abilities.update=false` → 原生直接进阅读态,不启动画布;
- 评论锚定:宿主「在文档中查看」→ 画布注入 `wemeet-navigate-comment(threadId)`(docs 侧已有 thread store 与 y-prosemirror 定位,仅新增一条消息命令);
- 内存:画布 WebView 只在编辑态创建、退出销毁(与常驻 tab WebView 互不干扰的真实隔离);**常驻 tab WebView 在新版原生主页就绪后即可退役**(见 4.9),App 里只保留「深链兜底/特殊块兜底」的轻量 WebView(`DocsViewerScreen` 体系)。

### 4.7 跨端协同(PC Web ↔ Android 原生)——本设计的第一等公民

docs Web 端(PC 浏览器)与 Android 原生端操作的是**同一个 docs 实例**(同一 Django/PG/S3、同一 Keycloak realm、同一 Hocuspocus 房间),数据层天然一致;**要设计和验证的是「端到端切换」的每一个入口**。PC 端分享的东西,Android 必须能无缝打开,反之亦然。

#### 4.7.1 身份与数据连续性(架构上已保证,列出依据)

| 依据 | 事实 |
|---|---|
| 同一用户 | PC 浏览器走 Keycloak SSO(`docs` client);Android 原生栈走「App Bearer → meet `docs/session/` 票据 → docs `session-from-ticket`」——二者最终都解析到**同一 `sub` 对应的 docs User**(`session_bootstrap._resolve_user` 按 sub/email 认人并懒建) |
| 同一文档树/同一份数据 | 列表/筛选/详情/tree 全部 docs REST、按登录用户可见性过滤 —— PC 与 Android 看到同一份数据,不存在客户端本地库分歧 |
| 邀请转正 | PC 邮箱邀请 → 受邀用户在 Android 登录同一账号那一刻,Invitation 自动转 DocumentAccess(`User.save` 首次登录钩子)——**「PC 分享给某人 → 那个人在 Android 可见」无需额外同步** |
| 权限变更即时生效 | 权限/链接权限/移动/删除都在 docs 后端裁决;Android 每次拉取都是后端现算(`abilities` 随响应下发,缓存仅作离线壳、非权威态) |

#### 4.7.2 跨端打开矩阵(PC → Android)

| PC 端的动作 | Android 端发生什么 | 打开路径 |
|---|---|---|
| Web(meet 框架内)docs「分享到聊天」 | IM 收到 doc-card 消息(带 docId+title+url) | **IM 卡片点击 → 原生 `DocDetailScreen`**;docId 直接取自卡片字段(不再依赖解析 URL),url 仅作 WebView 兜底 |
| docs 里复制链接(任意 reach)发给别人 / 发进聊天正文 | 聊天文本链接 / 深链 | 链接解析器(`docs.we-meet.online/docs/<uuid>` 等)→ 原生详情;**无权限 → ask-for-access 原生流**;无法解析 → 浏览器/WebView |
| 全局搜索(meet Web 或 App)命中文档 | 搜索结果点开 | 原生详情(`SearchApi.url` 解析 docId,兜底 WebView) |
| 会议纪要自动落 Doc(`create-for-owner` 已有)后在 IM 推送链接 | 会议历史/IM 内 doc 链接 | 原生详情 |
| 邮箱邀请 | 受邀用户在 Android 登录后 | 文档已在列表(4.7.1),刷新可见 |
| PC 批准了某人的 ask-for-access | Android 请求者 | 列表/详情刷新后恢复可见;详情页停在「申请访问」态 → banner 提示重试 |
| PC 端编辑(实时) | Android 阅读/编辑同一文档 | 4.7.4 的新鲜度与协同策略 |
| PC 调整链接权限/成员/移动/删除 | Android | 每次拉取后端现算,无本地权威态 |

#### 4.7.3 反向(Android → PC)

| Android 的动作 | PC 端发生什么 |
|---|---|
| 原生「分享到聊天」 | 与 Web 同一条 doc-card 链路(grantDocAccess + 卡片格式均已存在),PC 聊天显示同一卡片 |
| 复制链接 | 打开的是 docs Web(PC 浏览器同 realm SSO 免登) |
| 原生新建/编辑/移动/删除 | PC Web 打开同一文档,内容一致(编辑经同一 Yjs 存储) |
| Android 发起 ask-for-access | PC 端在 docs Web 收到申请并可审批(现有流) |
| 原生评论/解决/表情 | PC Web 评论列表同源呈现(REST 同一份) |

#### 4.7.4 实时协同与新鲜度(PC 在写、Android 在看/在改)

1. **阅读态新鲜度**(PC 编辑者持续改,Android 阅读者必须能看到):
   - 进入详情 / 切回前台(`ON_RESUME`)/ 下拉刷新 → 重拉 `formatted-content`(后端 ETag/304 兜底,无变化零成本,走 §4.5 缓存);
   - **阅读页停留期间**:前台可见时轻量轮询(每 ~30s 条件 GET,带 `If-None-Match`;304 不动、变了才重渲染,**不拉全量正文**)——避免「PC 改了半小时,Android 还在看旧文」;
   - 轮询发现变化 → 顶部轻提示「内容已更新」+ 自动重渲染(渲染器为纯函数,重建成本低)。
2. **编辑态协同**(Android 画布 = 与 PC Web 同一个 Hocuspocus room):
   - 画布内是 docs 编辑器本体,PC Web 与 Android 画布天然实时互见(光标/评论锚点/保存队列);
   - `?chrome=editor` 不改变 Hocuspocus 连接与保存语义 —— 这是保留 WebView 画布的核心红利:**跨端「多人同时编辑」零开发成本**。
3. **离线/冲突保护**(防 Android 写覆盖 PC 的实时编辑):
   - docs 后端已有锁:`COLLABORATION_WS_NOT_CONNECTED_READ_ONLY` + `PATCH content` 的 `websocket` 标志与 `can-edit`(无 WS 连接时单写者锁;他人在线而自己没有 WS 连接 → 403)——**这是唯一必要的保护开关**;
   - **部署核对项**:M2 上线前确认 docs 生产环境 `COLLABORATION_WS_NOT_CONNECTED_READ_ONLY=true`(`GET /api/v1.0/config/` 可查;App 读到关闭时应把原生 PATCH 降级为「请用画布编辑」);
   - Android 画布遵循 docs 自身保存队列(60s + 离页),**不在 App 侧另做 PATCH**,不引入第二套写路径。
4. **阅读→编辑切换**:进入编辑态前,画布按 docs 现有行为先拉最新 content 并 `applyUpdate`,无需 App 介入。

#### 4.7.5 外部系统链接(可选增强,不做强依赖)

- 手机系统里点击 `https://docs.we-meet.online/docs/<uuid>`(微信/Safari 等)→ 浏览器打开 docs Web —— **本身是正常路径**(docs Web 手机可用、同 realm SSO);
- 可选增强:给 App 加 `docs.<domain>` 的 `intent-filter`(+ `assetlinks.json` 验证),系统直接唤起 App 进原生详情;主场景不依赖它,列 M-later。

#### 4.7.6 跨端验收用例(每轮 M2/M3 上线必跑)

| # | 场景 | 期望 |
|---|---|---|
| 1 | PC Web 分享到聊天 → Android 聊天点 doc-card | 进**原生**详情,看到 PC 刚写的内容 |
| 2 | PC Web 实时编辑 ↔ Android 阅读同文档 | 阅读页 30s 内出现更新提示并重渲染,不丢内容 |
| 3 | PC Web 与 Android 画布同时编辑同一文档 | 双方实时互见(光标/保存),无丢字 |
| 4 | Android 画布切后台 → PC 改 → Android 回前台 | 画布与 PC 状态一致(Provider 重连补同步) |
| 5 | Android 断网画布编辑 → 恢复网络 | docs 保存队列补交;若 PC 在线编辑且 Android 早已断连 → 后端锁拒绝或安全处理,不静默覆盖 |
| 6 | PC 邮箱邀请 → Android 同一账号登录 | 文档出现在 Android 列表(首登录转 access) |
| 7 | Android 无权限点链接 → 发起申请 → PC 批准 | Android 详情刷新后进入可读/可编辑态 |
| 8 | Android 复制链接 → PC 打开 | SSO 免登直达同一文档 |
| 9 | PC 移动文档/改链接权限 | Android 刷新后列表与详情与 PC 一致 |

### 4.8 离线与缓存

- 列表/树:**沿用现有持久化模式** —— SharedPreferences/JSON 文件 + TTL(参照 `HistoryStore` 200 条 JSON、`UserDirectory` 1h TTL、`MediaResolver` ~50min 的既有先例),不引入 Room/DataStore;进入页先出缓存再刷新;刷新失败 `Snackbar`(内容不遮);
- 阅读态:文档 JSON + 图片按 etag/304 缓存(最大 LRU,如 50 文档);离线时出缓存 + 「离线,内容可能不是最新」标记;
- 写操作离线队列(本轮不做):新建/重命名/收藏等离线为「失败提示」+ 已存在错误语义;编辑离线靠 docs 自身 service worker(画布内工作);
- 回收站、版本、评论:仅在线(有缓存不承诺)。

### 4.9 兼容与回退

- **tab 层**:`DocsTabScreen`(常驻 WebView)保留为 **降级开关控制**:`BuildConfig.DOCS_NATIVE_ENABLED`(默认 true)+ 运行时异常(会话/格式兼容)→ 回退开关;老版本 App 不受影响;**回退时跨端入口保持现状**(IM doc-card → `DocsViewerScreen` WebView,行为不变),保证 PC 分享的文档在任何版本都能打开;
- **docs 镜像**:`?chrome=editor` + `sendToHost` 命令扩展(open-ai/export/share/navigate-comment/editor-dirty)均为**增量协议**,老 App 只发已知消息 → 兼容;
- **会话双栈**:原生栈与 WebView 栈各自引导,互不污染;登出清理两处 cookie;
- **服务端变更**:无(`docs/session/` 票据增强为可选,见 4.2)。

### 4.10 安全

- 原生栈保存 `docs_sessionid`/`csrftoken` 于 EncryptedSharedPreferences(与 `TokenStore` 同库),不落明文;
- 会话引导的最小化:票据一次性 60s,OkHttp 跟随 302 不转发 Cookie 到非 docs host(拦截器校验 host,参考 `DocsWebViewClient` 的允许域清单);
- 读 `config/`(AllowAny)不携带会话;媒体请求仅 `MEDIA_BASE_URL`。

### 4.11 UX 设计规范符合性(硬性要求,逐条映射)

**适用规范**:
1. `we-meet-android/docs/设计规范.md` —— M3 视觉与组件、token 唯一真源、共享组件、页面结构、文案、无障碍(WCAG 2.2 AA + 自订更严)、动效、§7 走查清单、§9 `checkDesignTokens` 10 条护栏;
2. `we-meet-android/docs/app-ux-device-matrix.md` —— 真机设备矩阵(横竖屏/字体缩放 1.3/1.5/2.0/平板折叠屏/insets/键盘/旋转);
3. 跨端 token 契约(We Meet 设计系统以 *we-meet* Web 的 `typography.tokens.json`/`shape.tokens.json`/`component-system.md` 为源,Compose 侧由 `core-design` 对齐——docs 模块**只用 `core-design`,不引入 docs Web 的 Cunningham 设计体系**;编辑画布是 docs Web UI,由 §4.6 主题/语言桥使其与 App 观感一致,文字/组件规范不覆盖它)。

#### 4.11.1 符合性矩阵(规范条款 → docs 模块落地)

| 条款(设计规范.md) | 落地 |
|---|---|
| §1 token 唯一真源(改样式只改 Color/Type/Dimens/Shape 四处) | 所有新页面 0 裸色值/0 裸 .dp/0 裸 sp;**文档内容区新增语义色成对进 `WeMeetExtras.docs.*`**(见 4.11.2),不在调用处 `isSystemInDarkTheme()` |
| §1.1 颜色分层 + 按语义选色 | 列表/详情/弹层全用 `MaterialTheme.colorScheme` + `extras.status`/`extras.im`;**danger 只用于不可逆操作**(删除/移动出权限/离开文档)并配二次确认;文档内容区**先复用** M3 面色与 `onSurfaceVariant`,确需新语义才按 §4.11.2 的四处路径新增(`Color.kt` 成对字面量 → `WeMeetExtras.docs` → 对比度单测) |
| §1.2 字号(M3 15 档 + 两档补充) | 正文 bodyLarge、副文 bodyMedium、时间戳 bodySmall、标题按 level 取 headlineSmall…titleSmall;代码块用 **bodyMedium + FontFamily.Monospace(不新增字号)**;任何可点击文字 ≥11sp;渲染器全部 sp 随系统缩放 |
| §1.3 8dp 网格 | 页面左右边距 `Dimens.ScreenPadding`;列表项间 `SpaceS`;分组间 `SpaceL`;**文档正文块间距是编辑器语义(与 docs Web 的 BlockNote 排版一致),属 renderer 豁免区,加注释 `// design-exempt: 文档内容区按编辑器语义排版` 并写理由** |
| §1.4 Shape/Elevation | `MaterialTheme.shapes.*`;浮层用 `Dimens.Elevation*`;不用 `Space*` 充当圆角 |
| §2 共享组件必用 | `WeMeetTopBar`(返回/标题单行截断)、`WeMeetLoading/WeMeetInlineLoading`、`WeMeetEmptyState/WeMeetInlineEmptyState`、`WeMeetErrorState/WeMeetInlineErrorState`(错误态带重试)、`PrimaryButton`(52dp、内置 loading、一屏仅一个)、`SecondaryButton`、`DangerButton`、`DestructiveConfirmDialog`;**业务组件(文档列表行/类型图标/评论气泡等)留 `feature-docs`**——遵循 core-design 的「只放所有 UI 都可能用到的东西」定位(先例:IM 会话行、群头像均在各 feature 自建) |
| §2.1 三态成组 | 每个列表/详情屏按 `when { loading&&empty -> WeMeetLoading(); error&&empty -> WeMeetErrorState(onRetry=…); empty -> WeMeetEmptyState(...); else -> 列表 }`;已有内容时的刷新失败用 Snackbar,不用整屏错误图;区块级用 Inline 三态 |
| §2.3 下拉菜单宽度 | 挂在图标/文字短锚点上的菜单一律 `matchTextFieldWidth = false` + 单项 `softWrap = false`(枚举项);「文档图标类型」「排序方式」「更多菜单」均适用 |
| §3 页面结构 | 根页不带 onBack,二级页必带;系统返回与顶栏一致;内容可滚动;评论/搜索/新建标题有输入 → 处理 ime padding |
| §4 文案 | 全部进 `strings.xml`(5 语言键对齐,中英必齐);业务代码无中文字面量(`cjk-literal` 护栏);空态说「现在什么情况 + 怎么才能有」(如「还没有文档,点 + 新建」);错误态说人话 + 给出路(「文档加载失败,检查网络后重试」);破坏性确认标题写后果、按钮写动作名(「删除后 30 天内可恢复」/「删除」) |
| §5.1 WCAG 2.2 AA | 新增颜色对**实算**:正文 4.5:1、图标/按钮/指示 3:1(4.11.2 给出目标对);所有功能性图标 `cd_` 前缀;装饰性图标显式 `null`;不靠颜色单独传达(文档只读/可编辑用图标+文字,不用只用颜色) |
| §5.2 自订更严 | 触控区 ≥48dp(`Dimens.MinTouchTarget`);正文 ≥11sp;深浅两套都过 |
| §6 动效 | M3 默认时值/曲线;loading >300ms 才显示;评论/展开用默认 transition |
| §7 走查清单 | 每新屏按 11 条逐条过(作为 PR checklist 硬性项,见 4.11.4) |
| §9 护栏 | `checkDesignTokens` 全模块扫描自动覆盖 `feature-docs`(10 条规则:raw-color/raw-color-named/raw-font-size/raw-dimen/raw-topbar/cjk-literal/text-color-slot/dropdown-menu-width/icon-button-description/icon-button-touch-target);**基线棘轮**:新模块违规 = 基线上升,必须 PR 说明;豁免一律 `design-exempt: 理由` 并显式说明豁免边界(只允许 renderer 的「文档内容区」两类:块间距与内容语义色) |
| app-ux-device-matrix.md 全项 | 见 4.11.3 设备矩阵适配与验收 |

#### 4.11.2 新增样式 token 与扩展点(`core-design` 的规范动作)

**原则:优先复用 `core-design` 现有 token,只有「语义确实需要」才新增;新增一律走规定的四处扩展点,禁止在调用处 `if (isSystemInDarkTheme())`。**

| 用途 | 策略 |
|---|---|
| 正文/标题/弱化文字 | `MaterialTheme.colorScheme.onSurface` / `onSurfaceVariant`;标题按 level 映射 M3 15 档(headlineSmall/titleLarge/titleMedium…),**不新增字号** |
| 链接 / 互链卡片 | `colorScheme.primary`(+ `onSurfaceVariant`),不新增 |
| 引用块 / callout / 代码块 / 表格容器 | **先试 M3 面色** `surfaceContainerLow/High/Highest` + `onSurface`/`onSurfaceVariant`(成对面色,对比度由 M3 保证);视觉区分不足再补**一对** `docs.emphasisContainer/onEmphasisContainer` |
| 评论锚定高亮 | 新增**一对** `docs.commentHighlight` / `docs.commentHighlightText`(唯一确定的新语义色) |
| 代码/表格边界线 | `colorScheme.outlineVariant`(结构线,非文字),不足 3:1 再补 `docs.divider` 一档 |

**新增色的规范动作(唯一路径)**:
1. `core-design/…/ui/theme/Color.kt` 加 `Light…`/`Dark…` 成对字面量(注释写明对比度计算值:文字 vs 底 ≥4.5:1、容器 vs 页面 surface ≥3:1);
2. `Theme.kt` 加 `data class DocsColors(...)` → `WeMeetExtras` 增 `docs: DocsColors` 字段 → `LightExtras`/`DarkExtras` 两处接线;
3. 对比度写进 `core-design` 的 `ColorContrastTest` / `FoundationTokensTest` 同款单测(先例:通话红绿、日历 reminder 的踩坑记录都在 Color.kt 注释里);
4. 消费侧一律 `WeMeetTheme.extras.docs.*`,不出现色值字面量(`raw-color`/`raw-color-named` 护栏自动覆盖)。

**无需新增**:字号(15 档 + LabelTiny/CodeDisplay 已有)、间距(`Dimens` 8dp 网格 + 文档块间距豁免)、Shape/Elevation(`JusiShapes`/`Dimens.Elevation*`)、图标(`material-icons-extended`,feature-docs 自行声明依赖,不改变 core-design 的 api 面)。

**共享组件边界(遵守 core-design 的定位)**:core-design 只放「所有 UI 都可能用到」的东西(其 build.gradle.kts 注释明示),所以:
- 直接用:`WeMeetTopBar`、`WeMeetLoading`/`WeMeetInlineLoading`、`WeMeetEmptyState`/`WeMeetInlineEmptyState`、`WeMeetErrorState`/`WeMeetInlineErrorState`、`PrimaryButton`/`SecondaryButton`/`DangerButton`、`DestructiveConfirmDialog`;
- 业务组件留 `feature-docs`:`DocListItem`(文档列表行)、`DocTypeIcon`、`DocCardActionsSheet`、`CommentBubble`、`VersionRow`、`DocReader`(渲染器)——遵循同套 token 与组件范式(先例:`feature-im` 的会话行/群头像不进 core-design);
- 跨模块复用:`core-directory` 的 `MemberAvatar`/`ContactPicker`、`feature-im` 的 `ForwardPicker`/`ImageLightbox` 参考(经 `DocsDeps` 注入,不跨模块直接 import);若未来日历/任务也要「文档卡片」,再评估提入 core-design/core-directory。

#### 4.11.3 设备矩阵适配(docs 模块专属项)

| 配置 | 要求与适配 |
|---|---|
| 竖屏默认 | 全文主流程(列表→详情→阅读→画布→评论→分享) |
| 横屏 | 阅读态利用宽度(正文容器 maxWidth ~640dp 居中、不 letterboxed);评论/分享/更菜单 BottomSheet 在横屏不拉伸到全宽;画布 WebView 跟随旋转 |
| 字体缩放 1.3/1.5/2.0 | 列表行标题/摘要可换行(two-line max + ellipsis);详情标题、评论正文可滚动;**正文渲染器全部 sp 单位随系统缩放**,2.0x 下仍可读且操作可达;弹层动作不被顶出屏幕 |
| 平板/展开折叠屏 | 列表与详情不因宽屏 letterbox;正文容器限宽居中;sheet/dialog 不无理由 edge-to-edge;画布在宽屏直接复用 docs 编辑器宽布局 |
| 折叠/展开中 | 阅读位置(scroll offset)、评论草稿、编辑态(WebView 存活,经 ViewModel/rememberSaveable 保留 docId+编辑开关)、多选态恢复 |
| 手势/三键导航 | 返回:阅读态返回列表;画布内先关浮层再退出(§4.6);insets 适配(顶部/底部/手势条) |
| 键盘 | 评论输入/搜索/标题编辑:imePadding + 焦点不被遮挡;画布内键盘由 WebView 自处理(验证一次) |
| 深浅两套 | 全部页面 + renderer + 画布主题桥(§4.6) |

#### 4.11.4 验收门(UX 部分)

- **PR 门**:`checkDesignTokens` 零新增违规 + §7 走查清单逐条勾选(挂在 PR 模板);
- **M1 门**:列表主屏/详情骨架/新建/更多菜单/搜索 —— 三态成组、WeMeetTopBar、菜单宽度、文案齐、无障碍图标;
- **M2 门**:renderer 全景(20+ 块/行内)走查 + 新 token 对比度实测表(把 4.11.2 的比值写进测试:`ColorContrastTest`/`FoundationTokensTest` 同款单测)+ 评论/分享/版本弹层横屏与缩放;设备矩阵「文档主流程」全项;
- **M3 门**:画布主题/语言对照、折叠屏横屏回归、TalkBack 抽查(原生 chrome 全可读;画布内 Web 内容抽查)、emoji/标题编辑可达性。

---

## 五、工作量估算与排期

> 口径:1 人日 = 8h;「1 名资深 Android + 0.5 名后端兼职」;估算**已含** docs 镜像侧小改(M3 的 `?chrome=editor`,3–5 人日,计入 M3);不含 M4 全原生编辑、不含三端同步发版的额外联调。
> 估算含测试(单元 + 快照 + 真机 E2E)、code review、i18n、无障碍、深色模式。

### M1 原生外壳(列表/新建/管理/搜索/深链/会话)

| 任务 | 人日 |
|---|---|
| `feature-docs` 模块脚手架 + 依赖接线 + 会话引导(`DocsSessionManager`,含 401/CSRF 重试) | 6–8 |
| 文档列表主页(筛选/排序/分页/三态/下拉刷新/长按多选) | 5–7 |
| 详情页骨架 + 元数据(标题/作者/时间) | 3–4 |
| 更多菜单操作(收藏/重命名/移动/复制/删除/恢复)/ 新建 / 回收站 | 5–7 |
| 模块内搜索页 | 2–3 |
| 深链路由 + WebView 兜底 + 与原 tab 的替换开关 | 2–3 |
| **跨端入口统一**:IM doc-card(`DocCardBubble` 仅带 docId/url,无 WebView 依赖)、全局搜索、会议纪要/日历引用 → 原生路由(`DocsViewerScreen` 收敛为仅兜底) | 3–4 |
| 分享到聊天入口(复用 `feature-im` `ForwardPicker`,经 `DocsDeps.shareToChat`) | 1–2 |
| i18n(5 语言键数对齐,中英必齐)+ 无障碍 + 深色 + 护栏对齐 | 2–3 |
| UX 走查与护栏对齐(第一批屏幕:三态/顶栏/菜单宽度/文案/§7 清单) | 1–2 |
| 测试(契约/单测/真机)+ review | 3–4 |
| **小计** | **33–46** |

### M2 原生阅读态 + 评论/版本/分享

| 任务 | 人日 |
|---|---|
| BlockNote JSON → Compose 渲染器(含 20+ 块/行内标记/降级规则/快照测试) | 10–14 |
| 渲染器接入详情页 + 图片/附件/媒体加载(Coil+cookie)+ 缓存(etag/304) | 4–5 |
| 评论/线程/表情(列表+发表+解决)+ 锚定跳编辑画布 | 5–6 |
| 版本(列表+恢复)+ 恢复二次确认 | 2–3 |
| 分享面板(链接权限/成员/邀请/申请访问/离开)+ 用户搜索(复用 `core-directory` `ContactPicker` + docs `users/?q=`) | 5–6 |
| 特殊块降级与兜底 WebView 通道 | 1–2 |
| **跨端新鲜度**:阅读态 ETag 轮询 + `ON_RESUME`/下拉刷新 + 冲突降级(`COLLABORATION_WS_NOT_CONNECTED_READ_ONLY` 未开时禁原生 PATCH)+ §4.7.6 用例 | 2–3 |
| **UX 规范**:文档内容 token 化(`WeMeetExtras.docs.*` 成对 + 对比度实算入单测)+ renderer 全屏走查(§7 清单)+ 无障碍(TalkBack/触控区/图标描述) | 2–3 |
| 测试(渲染器快照/契约/E2E)+ review | 3–4 |
| **小计** | **34–46** |

### M3 编辑画布 + 收尾

| 任务 | 人日 |
|---|---|
| docs 镜像 `?chrome=editor` + 协议扩展(docs 前端,5 个消息命令) | 3–5 |
| 画布 WebView 生命周期/主题/语言/脏检查/评论锚定注入 | 4–6 |
| 标题编辑原生化 + 保存状态机(与 60s PATCH 对齐) | 2–3 |
| 常驻 WebView tab 退役 + 兜底路径回归 | 2–3 |
| 全链路 E2E(含与 Web 端协同同开一行文档的回归)+ 上线灰度 | 3–4 |
| 设备矩阵回归(横屏/缩放/折叠屏/键盘,§4.11.3)| 1–2 |
| **小计** | **15–22** |

**合计:82–114 人日**。排期(2 人并行):M1 ≈ 3.5–4.5 周 → 可先行灰度;M2 ≈ 4–4.5 周;M3 ≈ 2 周;**总共 9–11 周**;1 人串行则 17–23 周。

**变更清单(三仓)**:
- we-meet-android:+`feature-docs`(新)± `MainTabScreen`(tab 替换开关)± `ui/docs`(缩减为兜底)± `AppNav`(深链)± `ApiClient`(会话单例)± `gradle.properties`(`DOCS_NATIVE_ENABLED`);
- we-meet-docs(镜像):`?chrome=editor` 收敛 + `sendToHost` 5 个命令(增量,向后兼容);
- we-meet(后端):可选:docs/session 票据字段增强(1–3 人日)。**主路径零改动**。

---

## 六、风险与决策点

| # | 风险 | 等级 | 对策 |
|---|---|---|---|
| 1 | y-provider 不可用 → `formatted-content` 500 → 阅读态崩 | 中 | 降级链:json→markdown→阅读态 WebView;监控 + 后端错误语义 |
| 2 | docs 会话引导与 WebView 栈 cookie 冲突 / 登出残留 | 低 | 双栈隔离 + EncryptedSharedPreferences + 登出清理(4.2/4.10) |
| 3 | BlockNote JSON 格式随 docs 升级变动 → 渲染器失配 | 中 | 渲染器版本化(docs `RELEASE_VERSION` 上报),未知块降级不崩;升级灰度 |
| 4 | 复杂表格/多列渲染成本超预算 | 中 | 先渲染基础表格 + 降级卡片;复杂内容走画布 |
| 5 | 「编辑画布」仍留有 Web 感 | 低(可接受) | 画布收敛到只留编辑器;标题/导航已原生 |
| 6 | 三仓同步(mirror 镜像更新滞后) | 中 | `chrome=editor` 增量协议 + UA 判据收敛;镜像未更新时画布退化为普通嵌入页(体验不变差) |
| 7 | M4 远期:全原生编辑被提上日程 | 中 | 先 Spike(yrs-JNI 通路 + y-websocket 最小客户端,5–10 人日),验证数据兼容后立项;阅读态渲染器是其天然前身 |
| 8 | 会话 12h 过期后的引导时机 | 低 | 401 自动重建;离线重试留队列 |
| 9 | 生产未开 `COLLABORATION_WS_NOT_CONNECTED_READ_ONLY` → 原生 PATCH 绕过冲突保护 | 中 | 部署核对(§4.7.4);App 读 `config/` 发现关闭 → 原生写路径降级为「用画布编辑」 |
| 10 | PC Web 与 Android 阅读态轮询频率 → 服务端压力 | 低 | 条件 GET(ETag/304)+ 30s 间隔,304 零负载;仅前台可见时轮询 |
| 11 | renderer 的「文档内容样式」与护栏/font-scale 的边界冲突(design-exempt 被滥用) | 低 | 豁免范围**限定 renderer 包**两类(块间距、内容语义色)+ PR 说明;新 token 对比度写进单测(参考 `ColorContrastTest`/`FoundationTokensTest`);字体一律 sp 随系统缩放,2.0x 走查 |

**拍板点**:
1. 是否接受「编辑画布 = WebView」(推荐);若坚持全原生 → 直接进入 M4 Spike 再排期;
2. 常驻 WebView tab 是否随 M3 退役(推荐退役,只留兜底);
3. `docs/session/` 票据增强(推荐做,省 1–2 人日 API 侧工作);
4. 阅读态渲染器的输入格式:BlockNote JSON(推荐,保真)vs Markdown(快 30% 体积小,但表格/样式降级)。

---

## 附录 A:事实速查(支撑本方案的关键代码事实)

| 事实 | 出处 |
|---|---|
| 内容 = base64(Yjs 更新),存 S3 `{doc_id}/file` | `we-meet-docs/src/backend/core/models.py`(content/save_content/file_key) |
| 版本 = S3 对象版本 | `models.py get_versions_slice`;`api/viewsets.py versions_list/detail` |
| 阅读转换端点 | `api/viewsets.py formatted_content`(json/markdown/html,y-provider 转换) |
| REST 认证 = session + CSRF | `impress/settings.py REST_FRAMEWORK`;前端 `fetchApi.ts`(csrftoken) |
| 票据引导(免 KC) | `core/api/session_bootstrap.py`;meet 侧 `core/api/docs_session.py` + `core/api/search.py(my-documents)` |
| 协同 WS | `servers/y-provider/src/servers/appServer.ts`(`/collaboration/ws/`)、`hocuspocusServer.ts`(cookie 鉴权、readOnly) |
| 编辑器栈 | `apps/impress/package.json`(BlockNote 0.51/Hocuspocus provider 3.4/Next 16);`doc-editor/components/BlockNoteEditor.tsx` |
| 评论锚定 = Yjs comment mark | `doc-editor`(DocsThreadStore:setMark('comment',{threadId}));`convertHandler.ts`(CommentsExtension 保 mark) |
| embed 协议 | `apps/impress/src/hooks/useIsEmbedded.tsx`(UA `WeMeetApp`、?embed=1、embedderLanguage/Theme)、`useEmbedShell.tsx`(wemeet-* 消息) |
| App 侧 WebView 集成 | `we-meet-android/app/src/main/java/com/we/meet/ui/docs/*`(DocsScreen/DocsViewerScreen/Bridge)、`ui/main/MainTabScreen.kt`(tab 5)、`data/api/DocsApi.kt`、`SearchApi.kt`、`p3-docs-app.md` |

## 附录 B:Yjs/JVM 生态事实(支撑「编辑面不可行」结论)

- `y-crdt`(Rust yrs)官方 **Kotlin 绑定 `ykt` 已停更**,README 指向 WIP 的 `y-uniffi`(UniFFI,官方稳定支持 Swift,Kotlin 为演进方向,功能未全暴露);ydotnet/yswift 等状态类似或仅部分平台;
- Compose 生态富文本库:`com.mohamedrejeb.richeditor`(Maven Central,基础 span/段落/列表/链接/HTML/MD 出入)、`halilozercan/compose-richtext`(展示 + 基础编辑)——均无块模型、无 Yjs、无协同、无 ProseMirror 兼容;
- `y-websocket` 协议(Hocuspocus)为标准二进制(lib0 编码 sync/awareness),需要自实现或依托 yrs;
- 结论:**没有可直接选型的 JVM 协同编辑器堆栈**;自建 = 编辑器 + CRDT 绑定 + 协议客户端三件套。

## 附录 C:Android 侧事实速查(支撑工作量与复用判断)

| 事实 | 出处(相对 we-meet-android/) |
|---|---|
| 6 模块 + composite `jusi-light-im`;无 DI 框架(WeMeetApp 手工装配 + feature `Deps` 接口);无 Room/DataStore | `settings.gradle.kts`、`app/src/main/java/com/we/meet/WeMeetApp.kt`、`feature-im/ImDeps.kt` 一族 |
| minSdk 29 / target 34 / compile 34;AGP 8.7.3、Kotlin 2.0.21、Compose BOM 2024.10.01 | `app/build.gradle.kts:48-50`、`gradle/libs.versions.toml` |
| 共享组件仅 6 个(WeMeetTopBar/三态/三按钮/DestructiveConfirmDialog);会话行/头像在各 feature 自建 | `core-design/src/main/java/com/we/meet/ui/components/*` |
| 规范 + 10 条自动护栏(`checkDesignTokens`,基线 + 棘轮)| `docs/设计规范.md`、`buildSrc/DesignLintTask.kt`、`config/design-lint-baseline.txt` |
| WebView 云文档 = 565+96 行;桥仅 `postEvent(String)` 一方法、4 个入站 type;UA 标记与主题 UA 双写 | `app/src/main/java/com/we/meet/ui/docs/DocsScreen.kt`、`DocsViewerScreen.kt` |
| 5 套 locale 键数对齐(values/zh-rCN/de/fr/nl)| `app/src/main/res/*` |
| 深链现状(三类 intent-filter:自定义 scheme + https 日历/会议号)| `app/src/main/java/com/we/meet/MainActivity.kt`、`ui/nav/AppNav.kt` |
| 既有 docs 代理:`/api/v1.0/docs/search/`(全局搜索)、`/docs/my-documents/`(分享选择器)、`/docs/session/`(票据)| `app/.../data/api/SearchApi.kt`、`DocsApi.kt`、`feature-im/data/DocsApi.kt` |
| WebView 真机验收矩阵 | `docs/app-ux-device-matrix.md` |
