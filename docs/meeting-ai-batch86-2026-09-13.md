# 第 86 批：原生行动项确认与任务跳转

人工纪要接入跟进任务区：显示已有转换状态、已删除回执；有任务访问权时可进入原生任务详情。行动项与当前人工 review/条数必须匹配，变更时阻止旧索引创建。任务权限由后端独立判断，不随纪要阅读权放开。

创建使用明确确认表单，标题可编辑，负责人必须手动选择；搜索清除旧选择，日期默认未设置，可用 Material 日期选择器设置/清除。纪要中的人名/日期文字仅作提示，不自动解析。确认文案明确创建任务、通知负责人及任务标题可见性，纪要权限保持独立。

未知请求使用加密原 key/body 显式核对，后台停止读取/操作，返回重新校验。只读/删除/旧版本不提供新增任务入口，不自动重新创建已删除任务。

验证：6 项新任务界面测试 + 6 项人工纪要 + 7 项资料页回归，共 19 项通过。Debug/test APK、默认开关构建、设计 token/diff 检查通过；浅色确认表单、深色删除态截图检查通过。无真实任务/通知/业务写入，无迁移或默认开关变更。继续会后问答与纪要交付，整体技术评审待完成。
