# Batch 99: native personal voice-translation workspace

The in-meeting More menu now opens personal voice translation behind `WE_MEET_ONLINE_AI_NATIVE=false`. The connected room and local participant SID bind the workspace, SDK data transport and private audio grants. A compact translation caption remains independent of hidden toolbars and reserves video space.

Starts preview language direction, continuous/two-way mode, translated voice and retention (default off). Listening is a separate explicit action. The meeting microphone is reused, with a visible reminder that original speech still follows meeting settings; no microphone permission or toggle is issued by translation controls. Active configuration is a compact read-only summary. Manual turns use ordered reliable commands and wait for authoritative completion.

Closing the panel preserves the ongoing translation; backgrounding revokes local playback, finishes a held turn with a bounded cleanup and clears transient text. A recovered unfinished turn is ended before another may begin. Unknown API requests retain original encrypted consent. Unknown data commands require stopping the affected runtime. While a stop/pending request is unresolved, listening and new speech cannot be re-enabled. Stop remains available when new starts are disabled. The optional saved-translation link opens its exact associated record; native archive browsing is still a following batch.

Validation: 20 distinct isolated UI/coordinator regressions passed (9 final private-workspace tests, 7 online recording, 4 translation coordinator). Enabled Debug/test APK and final default-disabled Debug builds, design-token/diff checks passed. Light confirmation and dark manual-state screenshots inspected; a layout pass replaced disabled configuration controls with readable active-state text. No real meeting, microphone, provider or deployment was exercised. Physical-device reconnect/subscription timing and connected-room layout remain deployment tests. Shared interpretation follows next.
