# 第 78 批：原生分阶段总结、自动纪要与速记查看

录音页和未锁定版本的记录详情页接入 RecordSummaryControls。仅有当前生成权限时显示操作；根据服务端 ready_stages 提供实时/速记/完整版按钮，活跃任务不重复新建，失败重试携带精确阶段和 attempt。分块进度、排队和等待原文状态独立展示。

自动纪要通过明确按钮开启，提示持续模型使用；服务端关闭新建能力后，已开启记录仍可停止。总结与自动化未知结果分别核对原请求，恢复前台不会自动发送。权限撤销、读取失败及离开前台会收起相应内容和操作。

录音页显示最近三份不可变纪要版本、暂定/转写中标识和精确原文引用，可进入全部纪要。绑定 canonical record 和 capture ID，不读取复用房间的最新结果。历史通知锁定版本时不显示生成控件。

验证：7 项新增 Compose 测试与 12 项记录/转写 UI 回归，共 19 项在隔离模拟器通过；覆盖阶段就绪、未知结果、开启/停止、权限撤销、失败重试、速记引用及后台清除。Debug/test APK 与 token 检查通过，浅色/深色/未知请求截图已检查。使用假 API、无真实模型/网络操作。实体设备与后端端到端测试仍由部署后验证；原生播放、任务等后续能力继续实现。
